/** Shortcut rows for Help screen and ? modal (single source of truth). */
export const KEYBOARD_SHORTCUT_ROWS: { keys: string; description: string }[] = [
    { keys: '?', description: 'Open keyboard shortcuts (when not typing in a field)' },
    { keys: '/', description: 'Search the cookbook from any tab' },
    { keys: 'Esc', description: 'Close dialogs, recipe modal, search, or image lightbox' },
    { keys: '← / →', description: 'Previous / next recipe when the recipe modal is open' },
];
